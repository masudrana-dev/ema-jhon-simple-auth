/**
 * 1. create a new project in console.google.com
 * 2. npm install firebase  npm install firebase
 * 3. create firebase.init.js and import getAuth to export auth
 * 4. Firebase settigns > go to authentication > enable email password auth
 * 5. create Login , signUp component , setup  router 
 * 6. attach form field handler and form submit handler
 * 7. install react firebase hooks 
 * 8. if user is created redirect to the expected page
 * 10.  useSignInWithEmailAndPassword for sign in
 * 11. create require auth component ===> check user exist track also userLocation 
 * 12. In route wrap protected component by using required Auth component  
 */


/**
firebase Hosting Steps:
1.npm install -g firebase-tools
2. firebase login
3.firebase init
4.firebase deploy
*/